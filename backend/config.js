const SECONDS = 1000;
const MINUTES = SECONDS * 60;
const HOUR = MINUTES * 60;
const DAYS = HOUR * 24;

const REFRESH_RATE = 5; //units

module.exports = {
  SECONDS,
  MINUTES,
  HOUR,
  DAYS,
  REFRESH_RATE
};
